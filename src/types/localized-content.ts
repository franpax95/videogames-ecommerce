export interface LocalizedContent {
  // id: number; // If necessary, conversion to number directly the id value
  // documentId: string;
  // locale: string;
  // createdAt: string;
  // updatedAt: string;
  // publishedAt: string;
  [key: string]: string;
}
